export enum SidebarTypeEnum {
    CONTENT = 1,
    HIT_TEST = 2,
    SETTING = 3,
}

export enum KnTypeEnum {
    VECTOR = "vector",
    RAG = "rag",
}

export enum KnbTypeEnum {
    RAG = 1,
    VECTOR = 2,
}
